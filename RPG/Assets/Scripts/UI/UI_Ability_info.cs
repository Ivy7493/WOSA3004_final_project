using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class UI_Ability_info : MonoBehaviour
{
    // Start is called before the first frame update
    public string Name;
    public Sprite Icon;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
